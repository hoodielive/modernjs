let percentValue = 5;

// let calculateFiction = (value) => { return value/100 * (100 + percentValue) }

let calculateFiction = (value, percentValue) => { return value/100 * (100 + percentValue) }

console.log(calculateFiction(300, 24.2));
