// Numbers

// Addition
let num = 1 + 1; // 2

// Subtraction
let sub = 4 - 9;

// Multiplication
let multiply = 4 * 9;

// Division
let divide = 4 / 9;

// Modulus
let modulo = 30 % 4; // 2

// Bit Shifiing
let bit_shift = 1 << 2; // Binary Math                      256  128  64  32  16  8  4  2  1

// PEMDAS Parantheses, Exponents, Multiplication, Division, Addition and Subtraction
let result = 1 + 3 - 2 * 4;

// Booleans
let is_employed = true;
let is_fired = !false;

is_employed = !true;

// Strings
let joinedString = "hello" + " " + "world";

// Comparison operators
let a = 0;
let b = 3;
let first_comp = 1 === a;

// === exact match, equality operator - checks for equality
// !== inequality ! (negation operator)

// comparison operators
console.log(1 <= 10);
console.log(1 >= 10);

// concatenation operator
let sample = "1, 2, " + 3;
console.log(sample);
console.log(typeof sample);

console.log("Hello, " + ["Irete", "tu", "!"]);

console.log(undefined == null);
console.log(null === undefined);

console.log(13 + !0);
console.log("13" + !0);

// String methods.
console.log("Oyeku Meji".charAt(6));
console.log("Oyeku Meji".substring(0, 5));
