let isDone: boolean = false;
let lines: number = 42;
let fname: string = "Cursion";

console.log(fname, lines, isDone);

const foo: string = "5";

log(foo);
log(foo);
log(foo);

/**
 * Logs a string.
 * @param { string } str The string to Log.
 */

function log(str: string): void {
  console.log(str);
}
